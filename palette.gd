class_name Palette
## The game's entire color palette. These four colors (plus alpha
## variants of them) are the only colors the game uses.

const BLACK := Color("0a0912")  # edges and borders, HUD text
const PURPLE := Color("70579c")  # enemies (remaining HP)
const PEACH := Color("e096a8")  # hit enemies (damage revealed), currency drops
const WHITE := Color("fff1eb")  # background
